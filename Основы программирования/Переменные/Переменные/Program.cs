using System;

namespace Переменные
{
    class Program
    {
        static void Main(string[] args)
        {
            byte playerArmor;
            short enemyHealth;
            ushort enemyAttack;
            int playerHealth;
            uint playerAttack;
            long spellRange;
            ulong spellDamage;
            string spellName;
            float spellCooldown;
            bool playerIsDead;
        }
    }
}
